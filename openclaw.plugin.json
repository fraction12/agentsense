{
  "id": "agentsense",
  "kind": "memory",
  "skills": ["./skill"],
  "uiHints": {
    "dbPath": {
      "label": "Database Path",
      "placeholder": "~/.openclaw/memory/agentsense.db",
      "advanced": true
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Automatically buffer conversation text for entity extraction via cron"
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Automatically inject relevant graph context before agent starts"
    },
    "maxRecallEntities": {
      "label": "Max Recall Entities",
      "help": "Maximum entities to inject into context during auto-recall",
      "advanced": true,
      "placeholder": "5"
    },
    "captureMinMessageLength": {
      "label": "Capture Min Message Length",
      "help": "Minimum message length eligible for capture",
      "advanced": true,
      "placeholder": "50"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dbPath": {
        "type": "string"
      },
      "autoCapture": {
        "type": "boolean"
      },
      "autoRecall": {
        "type": "boolean"
      },
      "maxRecallEntities": {
        "type": "number",
        "minimum": 1,
        "maximum": 20
      },
      "captureMinMessageLength": {
        "type": "number",
        "minimum": 10,
        "maximum": 1000
      }
    }
  }
}
